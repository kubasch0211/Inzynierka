package org.example.flaminogs.repository;

import org.example.flaminogs.entity.Multimedium;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MultimediumRepository extends JpaRepository<Multimedium, Integer> {
}
